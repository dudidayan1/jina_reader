apiVersion: v1
kind: Service
metadata:
  name: {{ include "jina-reader.fullname" . }}
  labels:
    {{- include "jina-reader.labels" . | nindent 4 }}
spec:
  type: ExternalName
  externalName: {{ .Values.gatewayServiceName }}
  ports:
    - port: 80
      targetPort: 80
